#include <iostream>

using namespace std;

int main()
{
	for (int y = 0; y < 20; i++)
	{
		for (int x = 0; x < 20; j++)
		{
			if (y = 0 && x != 20)
				cout << "#";
			if (x = 20)
				cout << "\n";
			if (y != 0 && y != 20 && 
		}
	}
}
